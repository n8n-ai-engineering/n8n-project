# FlowForge

**Self-hosted Low-code платформа автоматизации процессов с интегрированным ИИ**

Визуальный конструктор рабочих процессов, вдохновлённый n8n, написанный с нуля на Node.js, React и React Flow. Объединяйте HTTP-запросы, JavaScript-скрипты и ИИ-агентов в автоматизированные цепочки через интерфейс drag-and-drop — без зависимости от облачных сервисов.

---

## Возможности

| Функция | Описание |
|---|---|
| **Визуальный редактор** | Холст drag-and-drop на базе React Flow |
| **Дашборд воркфлоу** | Создание, переименование, удаление и переключение между процессами |
| **Импорт / Экспорт** | Сохранение воркфлоу в `.json`; загрузка готовых пресетов |
| **Серверное выполнение** | Движок обхода графа запускает всю цепочку на бэкенде |
| **Интерполяция переменных** | Обращение к выходу предыдущего узла через `{{input.field.path}}` в любом поле |
| **Узел HTTP Request** | GET / POST / PUT / DELETE с заголовками, телом и ИИ-автозаполнением |
| **Узел Code (JS)** | Выполнение произвольного JavaScript в sandbox-среде Node.js `vm`; доступны `input`, `axios`, `console` |
| **Узел AI Agent** | Системный промпт + пользовательский промпт (с переменными `{{input}}`), выбор модели, кнопка «Test» |
| **ИИ-конфигуратор HTTP** | Опишите запрос на русском языке → поля заполняются автоматически |
| **Триггер Webhook** | Уникальный URL для каждого воркфлоу; POST любого JSON запускает выполнение |
| **Триггер Schedule** | Планировщик на базе cron с пресетами (каждый час, каждый день…) или произвольным выражением |
| **Настройки API в интерфейсе** | Ввод ключа OpenRouter через UI — без редактирования файлов |
| **Произвольные ИИ-модели** | Любой идентификатор модели OpenRouter/OpenAI вводится прямо в узле |

---

## Технологический стек

```
Backend   Node.js 18 · Express 4 · lowdb (JSON file store) · node-cron · OpenAI SDK
Frontend  React 18 · React Flow 11 · Tailwind CSS 3 · React Router DOM 6 · Vite
AI        OpenRouter API (поддержка GPT, Gemini, Claude, DeepSeek и других моделей)
```

---

## Быстрый старт

### 1. Клонировать репозиторий

```bash
git clone https://github.com/YOUR_USERNAME/n8n-project.git
cd n8n-project
```

### 2. Настроить переменные окружения

```bash
cd server
cp .env.example .env
```

Откройте `server/.env` и укажите свой API-ключ:

```env
OPENAI_API_KEY=sk-or-v1-your-key-here
OPENAI_BASE_URL=https://openrouter.ai/api/v1
PORT=3001
```

Бесплатный ключ доступен на **https://openrouter.ai/keys** — большинство моделей имеют бесплатный тариф.

> Ключ также можно указать позже через встроенный интерфейс настроек (иконка ⚙ на дашборде) — изменения применяются мгновенно без перезапуска сервера.

### 3. Установить зависимости

```bash
cd ..          # вернуться в корень репозитория
npm run install:all
```

Команда запускает `npm install` в корне проекта, в директориях `server/` и `client/`.

### 4. Запустить сервер разработки

```bash
npm run dev
```

| URL | Сервис |
|---|---|
| http://localhost:5173 | Frontend (React + Vite) |
| http://localhost:3001 | Backend API (Express) |

---

## Структура проекта

```
n8n-project/
├── package.json              # Корень: скрипты dev и install:all (concurrently)
├── server/
│   ├── index.js              # Express-сервер — REST API + webhook-эндпоинт
│   ├── engine.js             # Движок выполнения: обход графа, запуск узлов, интерполяция переменных
│   ├── scheduler.js          # Менеджер cron-задач (node-cron)
│   ├── db.js                 # Общий экземпляр lowdb
│   ├── openai-client.js      # Динамический клиент OpenAI/OpenRouter (читает настройки из БД)
│   └── .env.example          # Шаблон переменных окружения
└── client/src/
    ├── App.jsx               # Роутер (маршруты Dashboard / Editor)
    ├── pages/
    │   ├── Dashboard.jsx     # Список воркфлоу: создание, импорт, удаление
    │   └── Editor.jsx        # Редактор холста, тулбар, панель вывода
    ├── components/
    │   ├── Sidebar.jsx       # Библиотека узлов (drag-and-drop)
    │   └── SettingsModal.jsx # Настройка API-ключа в интерфейсе
    └── nodes/
        ├── StartNode.jsx
        ├── WebhookNode.jsx   # Отображает уникальный webhook URL с кнопкой копирования
        ├── ScheduleNode.jsx  # Выбор расписания cron
        ├── HttpNode.jsx      # HTTP-запрос + ИИ-автозаполнение
        ├── CodeNode.jsx      # JavaScript sandbox
        └── AiTextNode.jsx    # AI Agent с combobox для выбора модели
```

---

## Интерполяция переменных

Любое текстовое поле с поддержкой переменных разрешается во время выполнения на основе выхода предыдущего узла:

```
{{input.data.current_weather.temperature}}   → 15
{{input.text}}                               → "Текст от ИИ"
{{input.payload.user_id}}                   → 42
```

**Пример цепочки:**

```
Start
  → HTTP GET  https://api.open-meteo.com/…?current_weather=true
  → AI Agent  "Напиши прогноз погоды для температуры {{input.data.current_weather.temperature}}°C"
  → HTTP POST https://api.telegram.org/bot<TOKEN>/sendMessage
              body: {"chat_id": "12345", "text": "{{input.text}}"}
```

---

## Справочник API

| Метод | Путь | Описание |
|---|---|---|
| `GET` | `/api/workflows` | Список всех воркфлоу (метаданные) |
| `POST` | `/api/workflows` | Создать новый воркфлоу |
| `GET` | `/api/workflows/:id` | Получить воркфлоу с узлами и рёбрами |
| `PUT` | `/api/workflows/:id` | Сохранить узлы, рёбра и название |
| `DELETE` | `/api/workflows/:id` | Удалить воркфлоу и отменить его cron-задачи |
| `POST` | `/api/run` | Запустить воркфлоу из редактора |
| `POST` | `/webhook/:workflowId` | Внешний webhook-триггер (тело запроса → выход webhookNode) |
| `POST` | `/api/ai-agent` | Прямой вызов языковой модели |
| `POST` | `/api/ai-config` | Генерация конфигурации HTTP-узла по текстовому описанию |
| `GET` | `/api/settings` | Чтение сохранённых API-учётных данных |
| `POST` | `/api/settings` | Сохранение API-учётных данных в БД |

---

## Импорт и экспорт

**Экспорт:** нажмите кнопку **Export** в тулбаре редактора. Текущий воркфлоу сохраняется как файл `название-воркфлоу.json`.

**Импорт:** нажмите **Import** на дашборде и выберите `.json`-файл. Новый воркфлоу создаётся автоматически, после чего открывается редактор.

Формат JSON переносим — воркфлоу можно передавать коллегам или публиковать как готовые пресеты.

---

## Планы развития

Следующие функции запланированы к реализации в будущих версиях платформы:

- **Нативные триггеры Telegram / VK** — получение сообщений и событий напрямую от Telegram-ботов и VK Callback API. Для локальной разработки требуется публичный IP-адрес или сервис туннелирования (ngrok / Cloudflare Tunnel); для production-среды — собственный сервер с доменом.

- **Маркетплейс сообщества** — централизованный каталог, в котором пользователи смогут публиковать и загружать готовые пресеты воркфлоу. Пресеты распространяются как версионированные JSON-файлы с системой рейтингов и импортом в один клик.

- **Расширенные ИИ-агенты на базе LangChain** — замена простого узла с одним промптом на полноценный LangChain-агент с поддержкой вызовов инструментов (поиск в интернете, интерпретатор кода), многошаговых рассуждений и сохранения памяти между запусками воркфлоу.

- **Корпоративная панель администратора** — поддержка нескольких пользователей с ролевой моделью доступа, централизованное управление API-ключами на уровне рабочего пространства, журналы аудита и квоты на использование ресурсов.

---

*Проект разработан в рамках учебной практики для демонстрации архитектуры low-code автоматизации, серверного выполнения графов и практической интеграции искусственного интеллекта.*
